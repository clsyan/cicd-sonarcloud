const { fib } = require("./index")

describe("Fibonacci test suite", () => {
	// it("should log 9 times", () => {
	// 	console.log = jest.fn();
	// 	fib(10)
	// 	expect(console.log).toHaveBeenCalledTimes(9)
	// 	expect(console.log).toHaveBeenLastCalledWith(55)
	// })
	it("should pass", () => {
		expect(1).toBe(1)
	})
})